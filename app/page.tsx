'use client';

import { LiveDigits } from '../components/live-digits';

/**
 * Deployed app. Uses the premium Astral dashboard layout.
 * Fully functional with real trading + login.
 */
export default function DigitsPage() {
  return <LiveDigits />;
}
